#include <stdlib.h>
#include <math.h>
#include <stdio.h>

int main()
{
  int v[15], i, maior, menor;
  
  //atribuir valor ao vetor 
  
  for(i=0; i<15; i++){
  printf("Digite o valor do vetor na posicao %d\n", i);
  scanf("%d", &v[i]);
  
  }
  // atribuir valor as variaveis maior e menor
  
  maior = v[0];
  menor = v[0];
  
  for(i=0; i<15; i++){
      
      if(v[i]>maior){
          maior = v[i];
      }
      else if(v[i]<menor){
          menor = v[1];
      }
  }
  
  printf("O maior e: %d e o menor e: %d", maior, menor);
    return 0;
}
